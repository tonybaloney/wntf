.. highlight:: shell

============
Installation
============

At the command line:

.. code-block:: console

    $ easy_install wntf

Or, if you have virtualenvwrapper installed:

.. code-block:: console

    $ mkvirtualenv wntf
    $ pip install wntf
